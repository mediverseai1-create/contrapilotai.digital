export function AfterSignature() {
  return (
    <section className="section">
      <div className="wrap">
        <div className="sec-head">
          <div className="sec-head-body">
            <div className="eyebrow">Organizational exposure doesn&apos;t end at signature</div>
            <h2>The contract keeps working. Its record should too.</h2>
            <p className="lede">
              Most contract damage happens months after signature, not at it — a renewal nobody
              cancelled, an obligation nobody owned, a deadline nobody was tracking. Every review
              ContractPilot AI runs extracts the renewal dates, payment obligations, and deadlines
              buried in the text and keeps them on record with that contract, in plain view —
              instead of scattered across email and file folders where they&apos;re easy to lose.
            </p>
          </div>
        </div>
      </div>
    </section>
  );
}
